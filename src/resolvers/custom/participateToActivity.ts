import { GraphQLContext } from "../../context";

export const participateToActivity = {
  Mutation: {
    participateToActivity: (_: any, args: any, context: GraphQLContext) => {
      // Implementation here
    }
  }
};
